// src/pages/marketplace-stub.js
// Route: /marketplace.html
// Placeholder only — TM3 owns the real Marketplace & Services module.
// This exists so sidebar/Quick Post navigation has somewhere real to go
// instead of a 404 or a dead alert.

import { renderSidebar } from "../shared/sidebar.js";
import { renderFooter } from "../shared/footer.js";

renderSidebar(null); // no sidebar nav item owns Marketplace yet — nothing highlighted
renderFooter();
